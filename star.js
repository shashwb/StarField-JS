class Star {
    float x;
    float y;
    float z;

    star() {
        // width of the window and height of the window
        x = random(0, width);
        y = random(0, height);
        z = random(0, width);
    }
}
